# global.R file for transmittance and absorbance learning module

# load packages
library(shiny)
library(shinythemes)

# set colors
palette("Okabe-Ito")

# load in the any files needed by server
set.seed(13)
